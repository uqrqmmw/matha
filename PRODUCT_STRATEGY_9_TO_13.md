# 數A 9→13 級分產品策略

## 結論

這個產品不該只是把講義搬到螢幕上，也不該用更多圖表製造「有在努力」的感覺。它的核心工作只有一件事：把每次失分轉成下一個最短、可驗證的練習動作，直到完整模擬能穩定站上 13 級線。

9→13 是大幅進步，系統不能保證結果；可控制的是把四類浪費壓低：不知道現在練什麼、看懂但沒有獨立做出來、猜中被當成會了、資料很多卻沒有形成下一步。

## 閉環

1. **校準**：級分只看完整的系統模擬或實體／補習班模考。自適應弱項練習本來就會刻意抽難點，答對率不能拿來直接換算級分。
2. **選擇**：首頁只給一個最高優先動作，排序為「到期訂正／重測 → 建立完整模考基準 → 到期大綱 → 跨章混合精選 → 觀念自述」。只有重複證據顯示嚴重斷裂才短期分章。
3. **輸出**：完整模考依正式 100 分鐘作答；日常混合與眼刷主練破題方向，不用單題速度製造假進度。
4. **診斷**：答錯記廣義錯因，AI 再補第一個錯誤步驟與錯法機制；答對但猜中也進隔日重測，避免假熟練。
5. **遷移**：看完詳解後立刻做獨立類題。訂正重算與獨立類題分開記，因為「照著寫會」不等於「換數字會」。
6. **保留**：第二／三級訂正先在 2 天後獨立重測，通過後再於 7 天後確認；看過詳解的當下重算不冒充保留。
7. **驗證**：13 級的暫定操作門檻是完整 20 題、100 分鐘模考達 72/100；連續三場達標才視為穩定，不以單場高分宣布成功。

## 0825n 已實作

- 內建題庫由 82 題增加到 363 題；新增固定公式核心變式並補入基準題。答案由可重算模板產生，不讓生成式 AI 決定正解。
- 目前登入後私有主庫可安全出題 4138 題，另有 374 題保留在補圖／圖形 QA 佇列；再加 363 題內建核心庫。題包逐檔驗 SHA-256，失敗即退回核心庫，不讓壞包污染作答狀態。
- 公開程式與私有題目完全分離：GitHub Pages 保持輕量離線核心，登入後才從唯讀 Supabase Storage 載入題庫；大量題目不再塞進 `app_state` 或公開 repo。
- 同模板以 `grp` 分組，單輪避免連續出同骨架；跨輪優先抽未做過、做過次數較少的變式。
- 首頁加入「現在只做這件事」，直接說明理由、預估時間與唯一主動作。
- 學習者模型在重置後從零蒐集證據；完整 20 題模考、隔日三級、破題方向、2／7 日保留、大綱與觀念自述分開計權，連續三場 72 分以上才列為穩定里程碑。
- 系統模擬以 Wilson 區間顯示小樣本不確定性；若已有實體模考，級分證據優先採近三場實體成績，並顯示場間範圍。
- 練習答對率改稱「練習脈搏」，只用來選弱項，不再直接換算級分。
- 答對後可一鍵標「猜中」；紀錄保留 `confidence: guess`，錯題間隔設為隔日重測，但不增加答錯次數。
- 類題紀錄新增 `independent-transfer`／`guided-redo`、來源錯因、單元與難度，可分析訂正是否真的遷移。
- 數據頁與首頁產生「這週最值得修的三件事」，整合精細 AI 錯法、弱單元、到期錯題與類題成功率。
- 紙本／補習班模考可用很少欄位記錄：分數、剩餘時間、最多三個失分單元、主要錯因與備註；這些結構化欄位會進修分建議與 AI 老師摘要。
- 原卷耐久性新增固定工作量壓力回歸：6 頁 1200 筆、重複補傳、刪除墓碑、80 分鐘心跳當機恢復與 400% 畫布上限都必須保持不遺失、不重複、不復活。
- 逐題 AI 詳批只有在「第一錯步引用」能於卷面轉錄中逐字核對時，才保留中高信心診斷；缺證據、證據不在轉錄中或短到無法定位時，一律降級且不污染弱點統計。
- AI 第一次簡批可只覆核當前頁的辨識答案、對錯與分數，覆核前後都留下稽核紀錄；不用因單頁誤讀重跑整卷。
- 隔日訂正明示「只看答案重想、保存重想、必要時看詳解重算、AI 驗證」四階段，降低同一畫面上不知道下一步的摩擦。
- 教材題可零成本回報一眼就會、單元不對或題目有問題；回報不算能力證據，疑問題停止推薦且可人工恢復。
- 老師檢視新增單頁摘要，整理成績變化、三級分布、主要弱點與建議討論題；進度頁改顯示能由原始紀錄核對的實際進步事件。

## 判斷是否真的有效

落後指標：

- 最近三場完整 20 題、100 分鐘模擬是否至少 72/100。
- 是否連續三場站上 13 級線，而不是偶爾一場。
- 實體模考的百分比與剩餘時間是否同步改善。

領先指標：

- 到期錯題是否在當天清完、畢業題是否增加。
- 「猜中」與沒有方向的題是否在下一次重測轉為能獨立找到方向。
- 獨立類題成功率是否上升；若只會 guided redo，代表仍停在看懂。
- 嚴重斷裂單元短期補洞後，回到混合題是否能辨認並獨立使用方法。
- AI 錯法機制是否由公式／方法錯轉為較少的計算失誤，再逐步下降。

## 下一階段

1. 24 本同版教材目前 10 本完成可用、14 本仍在 OCR／圖形 QA；依王老師路徑優先完成例題與章末簡單／中等／困難題，並以真實作答資料淘汰答案爭議、圖資不完整或鑑別度低的題。
2. 把實體考卷照片／手寫批改做成明確的匯入流程：AI 先擷取題號、單元、第一錯步與錯因，使用者一次確認後才入庫。
3. 建立「同一錯法是否復發」的時間序列，例如正負號、範圍邊界、公式套錯，而不只是總次數長條圖。
4. 以 116 學測正式範圍與當年度級距持續校正題型、配分與級分估計；系統的 72 分里程碑仍是訓練門檻，不等同官方預測。

## 風險邊界

- 4138 題私有題雖已通過結構與來源檢查，仍不等於每題都經真實學生作答驗證；系統應依爭議回報、異常正答率與人工抽查持續隔離問題題。363 題核心變式偏基本與中等題，兩者都不能取代高品質正式模考。
- 級分表是粗估，年度難度與配分會變。產品必須持續顯示不確定性，不能用單一百分比承諾四級分進步。
- 手寫停頓與 AI 第一錯步是輔助證據；最終仍以能否獨立、限時解出新題與完整模擬表現為準。
