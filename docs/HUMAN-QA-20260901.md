# 人類操作 QA（2026-09-01）

## 結論

本輪沒有要求使用者試用。以實際登入帳號、真實 Supabase 資料路徑和接近平板直／橫向的視窗，先在 localhost、再在正式 GitHub Pages 完整走過最容易讓人立刻生氣的主流程。QA 過程抓到並修正 5 個實際問題；正式站重跑後沒有請求失敗、HTTP 錯誤或 console error。

本文件是工程驗收，不冒充 Samsung Galaxy Tab 的 100 分鐘真機耐久證據。正式題庫內容仍只採用已通過既有 fail-closed 發布鏈的 1,294 題。

## 實際走過的流程

- 登入後從首頁進入原版模考；確認原卷列在模考分類最前面。
- 開啟 6 頁原卷，實際畫黑、藍兩筆，翻到下一頁並隱藏工具列。
- 重新整理頁面後回到同一回、同一頁，兩筆都還在。
- 開全新瀏覽器 context，不帶 localStorage／IndexedDB，再登入；只靠雲端重建同一回與兩筆筆跡。
- 開啟批改版、隔日訂正與逐題詳解；檢查紅筆不覆蓋印刷題目、詳解預設收合，橫向展開時使用獨立側欄。
- 從 1,294 題私有題庫產生 10 題練習；確認有 10 題、10 個不同原題圖雜湊，第一題原 PDF 裁圖已真正顯示。
- 以 900×1480 與 1480×900 檢查首頁、模考庫、卷面、訂正與練習畫面；沒有水平溢出。量測另抓到筆粗滑桿僅 32px 與數個 13px 工具字，修正後卷面可操作控制項均至少 44px、常用工具字至少 14px。

## QA 抓到並已修正

1. 私有題庫的文字都是安全提示，舊去重邏輯把不同原題誤判成同一題，導致「選 10 題」實際只出 1 題。現在原 PDF 裁圖題以 `stemAsset.sha256` 辨識，同圖才去重、不同圖保留。
2. 練習題渲染後漏掉圖片 hydration，原題會永久停在「原題載入中」。現在每次渲染都會執行 typeset／資產載入，並以圖片實際寬度驗證成功。
3. 首次下載 933 個題包時畫面像卡住，且可能讓人誤以為已退回舊核心題。先加上最多 8 路並行與真實進度，之後再修正根因：題庫改為依教材、每包最多 64 題，共 31 包；沒有只用進度條掩飾 52.767 秒等待。
4. 卷面工具列偏小、橫向詳解可能壓到題目。現在常用控制項至少 48px；詳解預設關閉，橫向開啟時限制為獨立側欄，不覆蓋掃描題面。
5. 同一個已簽核 release 重新分包時，舊版固定版本路徑會被新 bytes 擋住。manifest 與待補圖佇列已改為內容位址化路徑；舊包不可變、可回滾，新包先逐檔回讀再切 alias。

## 實測結果

- 正式站全新 Chrome context 冷載：3.334 秒，1,294 題／31 題包；修正前同路徑為 52.767 秒／933 題包，縮短 93.7%、約快 15.8 倍。
- 自適應練習：10 題，10 個不同原題雜湊；第一題圖片寬 2,070px，無載入提示殘留。
- 本機重整復原：同一 run、同一頁、2 筆筆跡。
- 全新瀏覽器雲端復原：2 筆筆跡，待同步 0。
- 網路請求失敗：0；HTTP 錯誤：0；console error：0。
- 自動測試：`npm test` 325 項（323 通過、2 跳過）、Python／PostgreSQL 23 項（1 跳過）、figure 383 項、Edge 47 項；失敗 0。

完整操作截圖、量測 JSON 與結果保存在：

`C:\Users\yenke\Desktop\數學檔案\matha-human-qa-20260831\artifacts`

## 正式交付驗證

- 程式版已提交並推送 `main`；同一 commit 的 CI 與 GitHub Pages 成功，Pages 信任檔與本機逐 bytes 一致。
- 原 933 包版曾由 Supabase App loader 以一般使用者 JWT／RLS 全量讀回 1,294／1,294 張原題圖，缺檔與雜湊錯配均為 0。2026-09-01 的內容等價重包逐題 JSON 比對完全一致；31 包版再完成首次部署→實際回滾至 933 包舊 alias→不同記錄最終重部署。最終部署後逐檔讀回 1,327 個版本物件，並由一般使用者 JWT／RLS 實載 31／31 題包與 1,294／1,294 題圖；全新 Chrome context 亦成功顯示個人化題圖。
- 正式網址 `https://uqrqmmw.github.io/matha/` 已重跑本文件全部操作，18 個畫面無小於 44px 的控制項、無水平溢出，雲端恢復與私有教材原題顯示皆通過。
