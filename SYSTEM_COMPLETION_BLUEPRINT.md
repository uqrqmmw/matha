# 數A特訓整體施工藍圖

更新：2026-08-29  
用途：唯一施工總覽。老師流程看 `TEACHER_WORKFLOW_V2.md`，產品原則看 `PRODUCT_STRATEGY_9_TO_13.md`，批次證據與技術細節留在 `docs/`。

## 1. 最高目標與完工標準

在 116 學測前，把數學 A 從約 9 級分提升到可重現的 13 級分。系統必須形成這個閉環：完整新卷校準 → 找出失分環節 → 隔日重想 → 官方詳解與 AI 第一錯步 → 2／7 日新題驗證 → 安排最能追回分數的混合任務 → 老師週報。

暫以 72/100 為 13 級分訓練線，不冒充當年度官方預測；最近三回未看過的正式結構卷都站上目標線，才算穩定達標。

完工必須同時滿足：

1. Galaxy Tab S10 Ultra 可穩定完成 100 分鐘作答、批改與隔日訂正；當機後完整恢復。
2. 首輪只顯示對錯、分數與正解；隔日重想後才可取得官方詳解與 AI 詳批，且不確定時不猜。
3. 先有 100–150 題平衡先遣庫，再完成 350–500 題 starter bank，最終擴至 1,200–1,500 題。
4. 每題都有原始題面、必要圖形、官方答案、來源頁碼、可靠 14 單元標籤與真人簽核。
5. 正式校準卷庫存覆蓋剩餘排程並保留 2 回；先取得 6 回，擴充目標至少 15 回。
6. 每個弱點、強項與推薦都能回到作答證據；一次錯誤不宣布弱項。
7. 首頁兩次操作內開始最重要任務；老師能在 2 分鐘內讀懂一頁週報。

教材散題組成的整回只作訓練，不得冒充未看過的正式校準卷或用來估級分。

## 2. 目前狀態與下一步

| 項目 | 現況 | 判定 |
|---|---:|---|
| 核心非 OCR 題庫 | 363 題 | 可補位，廣度不足 |
| 去筆跡候選 | 1,952 題 | 僅供審核，未發布 |
| 可綁官方答案 | 1,919 題 | 33 題隔離；待兩種真人審核與發布簽核 |
| 平衡 starter 候選 | 364 題 | 14 單元各 26 題；308 題含圖；11 批 hash-bound V2 雙 QA 台、可判分答案欄位與全量雜湊驗證已完成，尚未發布 |
| 含圖題 | 824 題、1,710 個圖資 | 必須保留原圖 |
| 完整卷來源 | 4 回 | 1 回已寫、1 回僅 19 題、第三回為下一回候選、115 學測詳解不完整 |
| 正式私有教材 | 0 題 | `verifiedCount: 0`，不可假稱已上線 |
| 自動測試 | Python 211/211；Web 259 項（257 通過、2 項私人 gold 條件跳過）；Edge 13/13 | 2026-08-29 全套重跑通過 |

已知問題：273 題章名空白，另有 OCR 錯誤章名；正式單元要由 `bookId`、印刷區段與人工映射重建，不能採信 OCR `chapter`。

14 單元 coverage matrix 與 364 題 starter 候選已建立；只採高信心 `bookId + PDF 頁碼` 映射入選。兩本跨主題書已回到原始 PDF 印刷章節頁核界，不採信 OCR 章名。每單元 26 題、共 308 題含圖，已切成 11 批並產出兩種人工 QA 工作包。全量驗證器逐題重算 batch、原題、清理題、移除區、官方答案及 review template 雜湊，364/364 一致。抽查發現的 `matrix-equation-p102-ex19` 像素漂移仍隔離；完整證據見 `docs/STARTER_BANK_COVERAGE.md`。

11 批另已產出 resumable hash-bound V2 單題整合複核台，同一畫面核對原題、去筆跡、移除區與官方答案，仍分別輸出兩份相容的具名審核 JSON。答案關卡強制真人把官方答案輸入為 App 可判分結構；空答案、單選多解、選項越界或缺小題不能通過。V2 schema 與來源 template hash 都納入 packet 身分，避免舊 localStorage 審核污染新版；35 題途中可另下載完整 checkpoint，恢復時核對 packet SHA-256、完整題號集合並清洗欄位，瀏覽器狀態遺失時不必重做。Windows 一鍵啟動器會先拒絕舊包、錯題數與錯 SHA 回應，再交給一般瀏覽器，關閉時停掉本機 server。11 包已核對 364 題總數；Batch 01 主頁及四種題目／答案資產皆經 localhost 實測 HTTP 200。一次只渲染一題並可全螢幕雙指縮放；所有舊 combined 複核台不可再用。

雙審核後的發布鏈已補齊：交集驗證器拒絕缺少或矛盾的結構化正解；發布準備器重驗 catalog PDF、題圖、答案圖、頁碼、單元與所有 SHA-256，並依 exact source hash 固定抽查 10 題；具名真人完成抽查後才可簽核。下載檔探索、交集、十題抽查、驗簽與 bundle 已串成可續作狀態機：拒絕錯批與歧義匯出，每階段先寫 partial 再原子換名，已完成階段只在 exact hash 相符時續用。Bundle 中題包、題圖與 manifest 都使用不可變 `releases/<releaseId>/...` 路徑；部署器先逐檔上傳回讀驗雜湊，最後才切固定 alias，並保存上一版 alias 供防覆蓋回滾。這只完成工程關卡；目前尚無真人 Batch 01 結果，正式題仍是 0。

目前可用素材仍有真實缺口：10 個單元缺少部分章末角色，12 個單元不足 3 個高信心來源區段。選題器以 30% 例題、20% 簡單、35% 中等、15% 困難為目標，缺料即明列 shortfall；只有第二來源存在時才強制單一書籍不超過 50%，不會用錯誤標籤或假來源填滿規格。

完整卷 hash-bound 清冊已建立於 `docs/FULL-PAPER-INVENTORY.md`；第三次模考為下一回候選。其答案已從公開前端移除，Supabase Secret 與後端交卷閘門已部署，線上實測未交卷 403、已交卷 200、回傳 20 題共 100 分且不呼叫 OpenAI；第一、二回因歷史相容仍含舊 key，兩回視為已公開且不再作新鮮校準。

第一回私人首輪批改 gold set 已完成並綁定來源 hash、人工 70 分真值與實際向量筆跡。固定 GPT-5.5 重測加確定性核分後：答案 20/20、狀態與配分 20/20、複選選項 30/30、圈題號／未作答陷阱 2/2、總分 70/100；紅筆再以已保存筆跡吸附後為 18/18。未作答與不確定題不再於印刷題面猜位置。

第一回 7 題真實失分題已逐題綁定學生作答與官方詳解像素，建立第二輪 hash-bound gold draft。第 3、4、16 題要求證據不足時 abstain；第 11、12、13、14 題有可核對的第一錯步真值。8 張官方詳解裁圖已放入私有 `matha-solutions`；正式 Edge Function v29 只在同回、同題已到隔日且雲端保存一次真實重想後簽發 15 分鐘網址，不呼叫 OpenAI。正式環境已驗證：未登入 401、直接公開路徑 400、短效簽名圖 200；來源圖不進首輪 response、前端、IndexedDB 或 Service Worker。

Supabase CLI 管理登入已確認可直接管理正確專案與 `openai-proxy v29`。7 題詳批 runner 已補成只走正式 Edge Function、逐題原子續跑且無法接收 OpenAI key；但首回歷史 run 的 7 題目前 `attempts=0` 且沒有 `retry` log，後端在扣額與模型呼叫前正確拒絕 403。不得修改歷史資料假造重想；本人在 app 留下真實重想後再跑 prediction。

學習者模型已把可信錯因歸入題意辨認、破題方向、建式、推理執行、計算符號、答案表達、隔日保留七段；同一題重複紀錄不會灌大，至少跨兩個不同題目才列為反覆流程斷點。斷點會進入教材選題理由、AI 個人化上下文、學習模型與老師單頁摘要；單一錯題仍只保留為個別證據。

首頁已縮成「現在只做這件事」與最多兩個真的到期閉環；不重複主要任務、不用四張常駐卡填滿上半頁，王老師訓練規則改為預設收合，S10 Ultra 橫向不再為收合內容預留大塊高度。

原版卷分析與老師單頁已依官方配分拆出本回失分、只看答案重建、看詳解後重建、尚待訂正四種分數；「已重建」明示不等於下次穩定拿回，仍需 2／7 日新題或後續正式卷驗證。

教材精選已把正式卷官方配分、失分是否尚未訂正／僅在訂正中重建、目前可學會程度與估計作答時間合併為相對價值排序；至少同單元跨兩題失分才啟用，單題失誤不宣告弱項，也不顯示虛構的成功機率。每輪仍限制同來源最多兩題，並以模板骨架去重。

新作答與原卷訂正現在都明確保存辨認、方向、建式、執行、計算、表達六個流程欄位；每筆證據另記來源、信心、時間、原句或逐字卷面證據。本人輸入、答案結果、未驗證 AI 摘要與通過證據閘門的詳批彼此分開；低信心詳批不入弱點，舊資料才以文字分類作相容備援。支援級別與 2／7 日保留仍由既有訂正狀態保存。

老師檢視版已可逐題新增具名 `teacherOverride`：姓名、依據來源、理由、單元與第一流程斷點都會保存；AI 原判和每次老師修正維持不可覆寫歷史，跨裝置採逐筆聯集。後一筆只改某個欄位時不會清掉其他既有修正，最新逐欄判定才進入選題、學習模型與老師摘要；改為未知也會停止沿用舊 AI 推測。

首頁任務已加入滾動七日最低頻率：每週新增 2 個十一單元空白默寫（另照常處理兩日重測），以及 2 張基本定義自述；每天最多一張定義。達標後不再出現，只有未達標或到期時才占用最多兩個閉環名額；正式卷隔日訂正、眼刷第二天、保留重測與到期完整卷仍有更高優先權。

老師單頁已加入最近三回合格正式卷的分數趨勢、目前有證據的「仍無方向」題號，以及老師修正後的後續驗證狀態。修正必須在同單元兩道不同的新題答對且沒有後續失敗，才標成已驗證；後續失敗標成仍卡住，單元未知或證據不足維持待驗證。頁面只列下週最優先討論的三題，不把同題重做冒充遷移成功。

正式使用前診斷已完成：新開原版模考會先實測 IndexedDB 寫讀刪、救援檔能力、當版 Service Worker、私人 Supabase session、狀態與筆跡補傳、全部私有原卷 PNG，以及未交卷答案必須回 403。七個必要 ID 缺一即阻擋建立新考試；既有考卷仍可直接續寫與救援。檢查十二小時或換版後失效；答案閘門測試不呼叫 GPT。題庫未真人發布、持久儲存承諾與完整卷庫存只列注意，不冒充正式可用題庫或不相關的開考故障。

原卷工作台 0829n 已內建有界、零 AI 的真機驗收紀錄：直接量測翻頁完成 P95、落筆到 IndexedDB 成功的延遲與失敗、canvas 尺寸／同時層數、待保存量、可取得時的 JS heap、開啟／恢復次數及 PDF 產生。事件與樣本各有固定上限，紀錄會進入救援檔；首輪批改頁可查看並匯出 JSON。這只補足工程證據收集，不會把尚未執行的真機測試冒充通過。

**目前下一個唯一動作：在 Galaxy Tab S10 Ultra 用 0829n 一鍵通過開考檢查，實際完成第三回 100 分鐘作答；途中至少暫停離開並恢復一次、使用觸控翻頁、寫滿正常作答量，交卷後輸出批改卷 PDF 並開啟「真機驗收」匯出 JSON。隔日再走完重想、官方詳解與 7 題 AI 詳批。這一次真實使用會同時取得真機穩定與第二輪詳批評測所缺的證據。完整卷補到 6 回與 Batch 01 題庫發布仍分別需要王老師提供新來源及具名真人 QA；在取得前不偽造完成。**

## 3. 關鍵路徑

### P0-A：正式校準卷與真機穩定

- [x] 建立 hash-bound 完整卷庫存；第三次模考選為下一回候選，115 學測列後備候選。
- [x] 完成答案防洩漏上線：第三回答案只在伺服器端，交卷狀態由雲端 `app_state` 驗證；線上 403／200 與 20 題 100 分已驗證。第一、二回只作歷史相容，不再列新鮮卷。
- [ ] 先補到 6 回，再補到至少 15 回且覆蓋剩餘排程與 2 回備用。
- [x] 原卷已有增量筆跡日誌、IndexedDB、雲端補傳、救援檔及 6 頁／1,200 筆壓力測試。
- [x] 新開原卷前自動跑七項 fail-closed 安全檢查；版本或檢查逾十二小時即重跑，既有考卷不因診斷失敗失去救援入口。
- [x] 0829n 自動保留真機驗收數據並可匯出 JSON；量測有固定上限且不呼叫 AI。
- [ ] 用真實 Galaxy Tab 完成一回端到端驗收：放大、書寫、翻頁、暫停、恢復、交卷與 PDF。

驗收：不得丟筆跡、扣錯時間、卡死翻頁或把未保存顯示成安全。100 分鐘後仍可書寫；停止書寫 2 秒內顯示本機已保存；翻頁互動延遲 P95 ≤ 500 ms，記憶體、canvas 與事件監聽不無界成長。沒有新來源時可做混合訓練，但不得新增級分基準。

### P0-B：AI 批改可信

- [x] 用第一回的 20 題人工真值建立首輪 gold set；AI 只擷取答案，官方 key 確定性核分。2026-08-29 固定 GPT-5.5 重測達答案、逐題分數與總分 100%。
- [x] 複選逐項處理勾選、錯選與漏選；紅筆以保存的向量筆跡定位，未作答／不確定不在印刷題面亂畫。首回複選 30/30、紅筆 18/18。
- [x] 第一回 7 題私有綁定官方答案與詳解裁圖；首輪 response、公開前端、IndexedDB、Service Worker 均不含詳解，Storage 直接路徑不可讀。
- [x] 第一回全部 7 題真實失分題已建立固定、來源雜湊綁定的第二輪 gold draft；不為湊數偽造個人弱點。
- [ ] 跑 7 題實際 GPT-5.5 詳批輸出並通過 precision、coverage、abstain、正確前綴與具名真人發布簽核。
- [ ] 隨實際作答累積至少 30 題真實詳批 gold set，覆蓋算式、圖形、複選與空白題。

驗收分開計算：清楚答案的 AI exact match ≥ 95%；使用者確認後逐題與總分 100% 正確；無證據高信心診斷 0；對卷面足以定位第一錯步的案例，診斷 precision ≥ 90%、coverage ≥ 60%，其餘 abstain 且不寫入弱點。

### P0-C：正式教材題庫

- [x] 產出 14 單元 × 角色 × 難度 × 有圖與否的 coverage matrix，建立 `bookId → 正式單元` 與印刷區段映射；provisional 區段不自動入選。
- [x] 選出 364 題平衡 starter 候選，每單元 26 題；以中等題為主要目標並明列角色不足。
- [x] 排除已知相同／不安全題面；素材允許時單一書籍不超過 50%，並明列不足 3 個來源區段的單元。有圖題沒有因處理成本被排除。
- [x] 11 批候選的 hash-bound V2 像素／答案 QA 台已產出，364 題所有資產與 manifest 雜湊已離線驗證一致；答案通過時必須有真人輸入的最小可判分結構。
- [x] 工程發布鏈完成：雙審核交集維持 `releaseAuthority:false`，重新核對原 PDF 與資產，固定抽查 10 題，exact-hash 具名簽核，原子續作狀態機、版本化 bundle、alias 最後切換與防覆蓋回滾均有 fail-closed 測試。
- [ ] 由具名真人先完成 Batch 01 的 35 題像素 QA、答案數學 QA 與正解輸入，再完成 10 題發布抽查及最終簽核。
- [ ] 用正式 Supabase 私有 bucket 發布 Batch 01，核對線上題數與雜湊，實測切換與回滾後才標成正式可用。

驗收：正式數量與 manifest 一致；任一題可追到原書、頁碼、原題、圖形、答案與審核者。缺圖、錯綁、殘留筆跡、缺答案或未簽核題永不進出題路徑。

### P1：個人化、首頁與老師週報

- [x] 由可信錯因建立七段解題流程模型；至少跨兩題才列反覆斷點，並進入選題、AI 上下文與老師摘要。
- [x] 每題明確保存單元辨認、第一方向、建式、執行、計算、表達、支援級別及 2／7 日保留；新證據含來源、信心、時間與原句，舊文字只作相容備援。
- [x] AI 錯因只有卷面證據通過才入模；人工改判留稽核歷史。老師調整另存具名 `teacherOverride`、時間、理由與來源，跨裝置不互蓋。
- [x] 推薦依跨兩題以上的正式卷配分證據、可學會程度與時間排序；限制同來源、同模板重複，分章介入只由混合證據觸發。
- [x] 首頁只保留目標進度、「現在做」與今日最多兩個必要閉環；規則預設收合且不為空內容保留版面。
- [x] 保留十一單元默寫與定義自述的最低週頻率；每週 2 個新大綱單元（另含兩日重測）與 2 張定義，只在到期時進首頁且不打斷高優先閉環。
- [x] 原版卷與老師單頁依官方配分顯示失分回收狀態，且不把訂正重建冒充穩定得分。
- [x] 老師一頁週報顯示最近三回趨勢、證據明確的無方向題與老師回饋採用情況；需兩道後續不同新題才算驗證，且只列下週優先三題。

驗收：每項診斷與推薦都有來源，證據不足顯示未知；首頁兩次操作內開始任務；老師 2 分鐘內知道本週最該討論的三件事。

## 4. 最快交付階段

| 階段 | 交付結果 | 工程時間；不含真人等待 | 主要阻塞 |
|---|---|---:|---|
| M0 工程基線 | 363 題、原卷工作台、救援、自動測試與真機量測報告 | 已有，僅可試用 | 真機 100 分鐘尚未執行 |
| M1 批改可用 | 首輪已通過；官方詳解安全閘門與 7 題詳批真值已完成 | <1 日 | 實際詳批評測與真人簽核 |
| M2a 第一批正式教材 | 364 題候選、11 組 hash-bound V2 雙 QA 台、可判分答案與安全發布／回滾鏈已完成 | 工程已完成 | v4 Batch 01 的 35 題真人 QA、10 題抽查與發布簽核 |
| M2b starter bank | 364 題候選依批次晉級正式私有庫 | 不再等核心工程 | 真人 QA；稀缺角色與來源不足維持明列 |
| M3 核心完工 | 30 題真實詳批 eval、分數模型、精簡首頁、週報、6 回新卷 | 1–3 週以上 | 真實錯題、新卷與使用資料 |
| M4 題庫充足 | 1,200–1,500 題、至少 15 回且覆蓋排程的新卷 | 持續分批 | 不阻擋 M1–M3 使用 |

被真人或來源阻塞時縮小批次或轉做另一條 P0，不降低品質門檻。

## 5. 每週學習閉環

- 原則上 1 回未看過的 20 題、100 分鐘正式卷；庫存不足時不以組卷冒充。
- 隔天完成整回三級訂正。
- 1–2 回完整眼睛刷題，只找方向、不展開計算。
- 2 批各 8–10 題混合精選，優先到期保留與可追回分數。
- 穿插十一單元默寫與定義自述；一次只做短任務。
- 週末輸出老師一頁報告，下一週依新證據與老師回饋調整。

只有正式卷庫存足以覆蓋剩餘排程並保留 2 回時，後期才提高到每週 2 回；現在不以速度為主。

## 6. 不可違反的規則與工作順序

1. 原始像素、官方答案與官方詳解才是真值；OCR 只作索引，生成式解答不能取代來源。
2. 失敗時封閉：缺圖、缺答案、錯綁、殘留筆跡、低信心或未簽核一律隔離。
3. 正式校準與日常訓練分開；已看過、19 題或非正式結構卷不進級分趨勢。
4. 工程／教材離線管線不可使用內建 browser，不可使用 OpenAI API，不可重跑已付費 YesScanner。
5. 正式 app 的必要 AI 批改只走既有後端、固定 GPT-5.5，快取並避免重複呼叫。
6. 暫停遊戲化、速度工具、舊功能復活、全面 1,919 題盲審及不影響作答的視覺微調；M3 前不開自然科施工。
7. P0 是資料遺失、錯題／錯答案、錯分、詳解洩漏、未簽核出題或主要流程失敗；先修 P0，再處理平板、診斷與推薦等 P1。
8. 每次先讀本文件與 `git status`，執行第一個未阻塞 P0；沒有來源或真人證據就明列 blocked，不用 AI 猜完成。
9. 修改前定義驗收案例；提交前跑 Python 與 Web 全套測試，核對私有素材未進公開 repo、版本戳與差異正確。
10. 更新本文件的狀態與下一個唯一動作後，以小 commit 推送 `main`。

責任邊界：Codex 負責程式、資料管線、選題、QA、部署與文件；使用者負責真機作答、確認 AI 誤讀與必要的具名 QA／簽核；王老師負責訓練路徑、新模考來源與數學爭議。
